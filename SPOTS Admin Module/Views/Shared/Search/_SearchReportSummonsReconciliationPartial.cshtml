@model SPOTSAdminModule.ViewModels.SummonsStatisticsUsersViewModel

<div id="tabs">
    <ul>
        <li>
            <a href="#tabs-1">Search By</a>
        </li>
    </ul>
    <div id="tabs-1">
        @using (Ajax.BeginForm("SummonsReconciliation", "Report",
                    new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "summonsReconciliationResultTable" }))
        {
            <div class="row">
                <div class="col-md-2">
                    Date From
                    @Html.TextBoxFor(m => m.SearchModel.SearchFromDate, new { @class = "datepicker", @placeholder = "Enter from Date" })
                </div>
                <div class="col-md-2">
                    Date To
                    @Html.TextBoxFor(m => m.SearchModel.SearchToDate, new { @class = "datepicker", @placeholder = "Enter to Date" })
                </div>
                <div class="col-md-2">
                    Officer ID
                    @Html.TextBoxFor(m => m.SearchModel.SearchOfficerId, new { @placeholder = "Enter Officer ID" })
                </div>
                <div class="col-md-2">
                    <input class="" type="submit" />
                </div>
            </div>
        }
    </div>
</div>