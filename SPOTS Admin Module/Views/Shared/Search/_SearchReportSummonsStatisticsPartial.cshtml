@model SPOTSAdminModule.ViewModels.SummonsStatisticsViewModel

<div id="tabs">
    <ul>
        <li>
            <a href="#tabs-1">Search By</a>
        </li>
    </ul>

    <div id="tabs-1">
        
            @using (Ajax.BeginForm("SummonsStatistics", "Report",
                            new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "summonsStatisticResultTable" }))
            {
                <div class="row">
                    <div class="col-md-3">
                        <label class="control-label">Date From</label>
                        <div class="input-group">
                            @Html.TextBoxFor(m => m.SearchModel.SearchFromDate, new { @class = "datepicker-my form-control", @placeholder = "Enter from Date" })
                            <span class="input-group-addon btn" style="cursor:pointer;">
                                <span class="glyphicon glyphicon-calendar"></span>
                            </span>
                        </div>
                    </div>

                    <div class="col-md-3">
                        <label class="control-label">Date To</label>
                        <div class="input-group">
                            @Html.TextBoxFor(m => m.SearchModel.SearchToDate, new { @class = "datepicker-my form-control", @placeholder = "Enter to Date" })
                            <span class="input-group-addon btn" style="cursor:pointer;">
                                <span class="glyphicon glyphicon-calendar"></span>
                            </span>
                        </div>
                    </div>

                    <div class="col-md-2">
                        <input class="form-control" type="submit" value="Search" />
                    </div>
                </div>
            }
        </div>
    </div>
